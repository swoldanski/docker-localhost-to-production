apiVersion: v1
kind: Service
metadata:
  name: {{ include "l2p.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "l2p.name" . }}
    helm.sh/chart: {{ include "l2p.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "l2p.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
